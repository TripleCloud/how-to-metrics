---
name: how-to-metrics
description: "Analyze, review, design, implement, and modify operational Metrics instrumentation, collectors/exporters, metric queries, and recording/alert rules. Enforce clear semantics, bounded cardinality and overhead, controlled database IO, and failure isolation. Use for 打点/埋点/指标采集/监控指标 correctness and cost work; not product-analytics event tracking, financial KPIs, or dashboard styling alone."
---

# Metrics 正确打开方式

让指标能够可靠回答业务问题，同时使采集与上报成本可控。将规则用于实际代码、查询和配置决策，不要只复述原则。适用于 Prometheus、OpenTelemetry 及其他指标平台；先确认实际 SDK、版本、导出方式及后端语义，不把 Prometheus 约定机械套用到所有平台。

## 任务范围与入口

- **Analyze / 解释 / 排障：** 还原指标的数据来源、生命周期和消费方式，给出证据支持的结论与未知项。
- **Review：** 审查指定代码或变更，优先报告实际缺陷；只读请求不自动变成代码修改或系统重构。
- **Write / Modify：** 在用户授权范围内实现或修复指标，验证生产者与消费者语义一致，交付实际变更。
- **查询、告警、指标文档：** 只应用相关约束。查询任务不自动新增埋点，文档任务不自动部署告警。

从当前任务定位仓库/文件，读取适用的 AGENTS.md。优先复用既有库、注册方式、标签约定和部署流程。没有现成上下文时，先定位已提供的实现；缺少决定正确性的业务口径才澄清，不要求用户重复授权。线上负载测试、业务库查询、发布和通知不是本 skill 自动授予的权限。

按需读取：
- 涉及 SQL、RPC、快照、exporter、异步回调、后台刷新、采集压力或内存增长：读 [采集与成本](references/collection-and-cost.md)。
- 涉及标签、指标选型、PromQL、告警、语义迁移、审查判定和验证：读 [语义与验证](references/semantics-and-validation.md)。
- 需要核对 SDK/平台版本能力或引用依据：读 [官方来源](references/sources.md)，再查相关官方文档。日常工作不要求每次加载全部资料。

## 工作流程

### 1. 先确定问题与统计契约

在实现、查询或实际样本中确认以下信息；小改动用简短说明即可，无需制造完整表单：
`用途 → 观测对象 → 数据来源 → 记录边界 → 类型/单位 → 标签范围 → 聚合范围 → 重试/失败/重启语义 → 新鲜度 → 消费者`

特别区分请求尝试、逻辑操作、实际创建对象；到达与完成；服务端成功与用户可见成功；本地状态与全局状态。不要凭名称推断口径，找不到证据就标注未验证。

### 2. 沿完整路径查成本和行为

检查 **业务调用点 → 包装器/SDK → 采集回调 → 导出/缓冲 → 后端查询/告警** 中与任务相关的部分。业务调用点只有一次 inc，不代表采集回调没有查库；使用异步 SDK，也不代表构造标签、排队、重试和后台任务没有成本。

需要估算时，使用已确认或明确标注的假设：
- 主动查询频率：每轮查询数 × 重复采集实例数 × 抓取副本数 ÷ 周期；再检查单次查询的 IO、扫描量、连接和最坏并发。
- 标签组合上限、实例数、经典 Histogram 桶展开以及标签随时间更替带来的成本。
- 热路径调用频率、分配、锁竞争、队列、重试与内存上限。

不要将未知成本写成“无影响”，也不要没有证据就宣称会拖垮系统。

### 3. 采用与数据语义匹配的范式

**事件型：** 在可靠的业务边界复用已有结果，进行本地有界聚合；采集或周期导出由现有监控机制完成。业务路径默认不得为打点增加同步 SQL、RPC、网络上报或业务库写入。不要给每次内存递增新建线程/协程/任务。

**状态型：** 优先使用已有可靠内存状态、原生统计或维护中的聚合结果。全局存量不能用局部增减计数冒充。必要的主动查询须有成本评估、超时、并发和新鲜度设计；廉价采集可按抓取执行，昂贵统计可采用受控刷新快照。

**消费型：** 按指标类型、单位、观测对象和维度正确计算。指标缺失、零流量和旧数据须可区分；正确数据也可能被错误聚合成错误结论。

这些是默认工程约束；遇到显式的精确审计、主动探测、业务事件持久化等要求，应辨认不同的数据可靠性需求，说明取舍并选用匹配的机制，而不是把普通 Metrics 改成业务同步强依赖。

### 4. 审查或落地改动

共同约束：
- 标签应有限、稳定且有明确查询用途；禁止默认引入无界身份、原始路径、SQL、错误文本。必要的高基数场景须有容量与查询语义依据。
- 采集/导出运行时故障不得覆盖业务结果、吞掉原业务异常或造成无界积压。隔离遥测故障不等于隐藏采集故障。
- 采集失败不写成 0；保留快照要暴露最近成功时间与状态，避免旧值被误认作当前值。
- 指标仅注册一次或按现有生命周期管理。异常、取消和失败路径遵守计数口径；业务重试与重复采集不得导致意外重复统计。
- 修改名称、类型、单位、标签、桶边界或计数时机时，查找现有消费者；设计兼容/迁移并避免新旧指标重复聚合。
- 不声称零开销或统一行业阈值。预算依实际服务要求确定；低成本改动不强制引入缓存、新系统或全套压测。

Review 只报告有证据的缺陷：**位置 + 触发条件 + 实际影响 + 最小可行修复**。缺失信息列为待确认项；风格建议与 correctness/availability 缺陷分开。没有发现缺陷时直接说明审查范围和验证限制，不编造问题。

Write/Modify 选择满足需求的最小实现，保持相关指标、查询、说明一致。修复实际问题后停止，不顺手重构无关观测系统。

### 5. 做风险相称的验证并交付

验证被改动的行为，而非匹配代码文本。按风险覆盖计数边界、异常/取消、重试/幂等、重启、多实例聚合、零流量/缺失、快照过期，以及相关资源上限。读库、并发、基数或导出策略变化时增加对应成本/故障测试；普通有限计数器不必执行所有场景。

交付使用任务需要的粒度：
- Analyze/Review：结论、证据位置、影响和建议、未验证事项。
- Write/Modify：变更内容、最终口径、资源/兼容性影响、实际验证结果及剩余限制。
- 指标文档：先解释使用者为什么需要概念，再给示例，最后引入术语；不要突然堆砌“时序、基数”等名词。

区分本地验证、已配置、已部署和线上已验证。不能访问运行环境时，完成可做的代码/静态验证，明确剩余线上检查，不把静态配置当作上线证据。
